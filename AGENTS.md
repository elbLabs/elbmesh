# Elbmesh Agent Rules

Work in this repository must always use PPP as the standard project guidance source.

Before substantial coding, review, planning, or documentation work, load and follow `.opencode/skills/ppp/SKILL.md`, then use the `ppp_*` tools to resolve repo context and task guidance.

The source of truth is the PPP library under `.ppp/` plus `ppp.repo-config.json`.

Keep changes slice-focused, test-first where behavior changes, and aligned with the PPP task bundle for the work.
